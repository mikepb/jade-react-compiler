module.exports = function() {
  return React.DOM.figure(null, React.DOM.blockquote(null, "Try to define yourself by what you do, and you&#8217;ll burnout every time. You are. That is enough. I rest in that."), React.DOM.figcaption(null, "from @thefray at 1:43pm on May 10"));
};